import React from "react";

// Sass Import

const Button = () => {
  return (
    <div className="home__homeBtn">
      <a className="btn btn--primary" href="#">
        Sign in
      </a>
    </div>
  );
};

export default Button;
